#include<stdio.h>
void main()
{
	int a=3,b=2,max;
	int *p1,*p2;
	p1=&a;
	p2=&b;
    if(max<b)
    {
    	printf("it is  maximun number is %d",*p1);
	}else
	
	{
		printf("it canot the maximum number is%d",*p2);
	}

}
