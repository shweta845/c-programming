#include <stdio.h>
void main(){
    int a, b;
    printf("enter the two integer:-");
    scanf("%d %d",&a,&b);
    if(a==b){
        printf("the integers is same");
    }
    else{
        printf("the integer is not same");
    }
}
